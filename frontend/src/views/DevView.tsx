/**
 * DevView — developer toolkit panel content.
 * Thin wrapper around legacy DevPanel during Phase 3 migration.
 * TODO: Rewrite internals to use lib primitives.
 */
export { DevPanel as DevView, DevPanel as default } from '../components/panels/Dev/DevPanel';
export type { DevPanelProps as DevViewProps } from '../components/panels/Dev/DevPanel';

