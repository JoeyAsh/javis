/**
 * setupApiStore — helper for RTK Query endpoint tests.
 * Stub — will be filled in when the first RTK Query test lands.
 */

// TODO: implement when first RTK Query test is written.
export function setupApiStore(): void {
    /* placeholder */
}
